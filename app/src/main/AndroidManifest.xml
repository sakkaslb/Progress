<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.massoluciones.com.progress">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:label="Progress"/>
        <activity android:name=".LoginActivity" android:label="Progress" android:theme="@style/Theme.FloatingWindow.Popup"/>
        <activity android:name=".GoalsActivity" android:label="Progress"    android:theme="@style/Theme.MyFancyTheme" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MemoryActivity" android:label="Progress"    android:theme="@style/Theme.MyFancyTheme" android:parentActivityName=".MainActivity"/>
        <activity android:name=".ProgressActivity" android:label="Progress"    android:theme="@style/Theme.MyFancyTheme" android:parentActivityName=".MainActivity"/>
        <activity android:name=".SocialActivity" android:label="Progress"    android:theme="@style/Theme.MyFancyTheme" android:parentActivityName=".MainActivity"/>
        <activity android:name=".SnapshotActivity" android:label="Progress"    android:theme="@style/Theme.MyFancyTheme" android:parentActivityName=".MainActivity"/>
    </application>

</manifest>
